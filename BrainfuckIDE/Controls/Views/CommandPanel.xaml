<UserControl x:Class="BrainfuckIDE.Controls.Views.CommandPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:BrainfuckIDE.Controls.ViewModels"
             xmlns:interpreter="clr-namespace:BrainfuckInterpreter;assembly=BrainfuckInterpreter"
   xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:BrainfuckIDE.Controls.Views"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance {x:Type vm:InterpreterRunningViewModel}}"
             >
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Margin="5" MinHeight="30" MinWidth="100" Content="実行" Command="{Binding RunCommand}" CommandParameter="{x:Static interpreter:RunType.Nomal}"/>
            <Button Margin="5" MinHeight="30" MinWidth="100" Content="ステップ実行" Command="{Binding RunCommand}"  CommandParameter="{x:Static interpreter:RunType.OneStep}"/>
            <Button Margin="5" MinHeight="30" MinWidth="100" Content="一時停止" Command="{Binding RaisePauseReqestCommand}"/>
            <Button Margin="5" MinHeight="30" MinWidth="100" Content="停止" Command="{Binding RaiseStopReqestCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
